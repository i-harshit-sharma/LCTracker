{
  "name": "@workspace/scripts",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "recalculate-profiles": "tsx ./src/recalculate-profiles.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "version-bump": "bash ./version-bump.sh"
  },
  "dependencies": {
    "@workspace/db": "workspace:*",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
